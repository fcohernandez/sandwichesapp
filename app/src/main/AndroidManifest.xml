<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sandwiches">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logosandwich"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logosandwich"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".DetalleSandwich"
            android:parentActivityName=".Sandwiches"/>
        <activity
            android:name=".Sandwiches"
            android:label="@string/sandwichesLabel"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".About"
            android:label="@string/app_name"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>